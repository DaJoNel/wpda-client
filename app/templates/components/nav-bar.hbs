<nav class="navbar navbar-default navbar-fixed-top">
	<div class="container">
		<div class="navbar-header">
			<h1>{{#link-to 'application' class="navbar-brand"}}Waze Place Discovery &amp; Audit{{/link-to}}</h1>
		</div>
		<div class="navbar-right">
			{{auth-bar}}
		</div>

		<div class="row">
			<div class="col-sm-12">
				<ul class="nav nav-tabs">
					<!-- navigation menu -->
					{{#link-to "application" tagName='li'}}
						<a class="nav-link" {{bind-attr href="view.href"}}><span class="glyphicon glyphicon-list" aria-hidden="true"></span> View Places</a>
					{{/link-to}}
					{{#link-to "watch-place" tagName='li'}}
						<a class="nav-link" {{bind-attr href="view.href"}}><span class="glyphicon glyphicon-eye-open" aria-hidden="true"></span> Watch a New Place</a>
					{{/link-to}}
					{{#if auth.isLoggedIn}}
						{{#link-to "profile" tagName='li'}}
							<a class="nav-link" {{bind-attr href="view.href"}}><span class="glyphicon glyphicon-user" aria-hidden="true"></span> View Account</a>
						{{/link-to}}
					{{/if}}
				</ul>
			</div>
		</div>

	</div>
</nav>
